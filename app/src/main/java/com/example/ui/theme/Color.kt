package com.example.ui.theme

import androidx.compose.ui.graphics.Color

val DarkGray = Color(0xFF000000) // Pitch Black background from PineLauncher
val SurfaceDark = Color(0xFF0B0F0D) // Very deep black steel
val CardDark = Color(0xFF161616) // Solid dark gray card background

val EmeraldM3 = Color(0xFF00E997) // PineLauncher neon spring green
val EmeraldLight = Color(0xFF00E997) // PineLauncher neon spring green active
val EmeraldDark = Color(0xFF0B1E14) // Dark green contrast for text on neon buttons

val TextPrimary = Color(0xFFFFFFFF) // Crisp clean white
val TextSecondary = Color(0x99FFFFFF) // Secondary transparent white (equivalent to copy(alpha=0.6f))
val DividerDark = Color(0x33FFFFFF) // Thin glass border (equivalent to copy(alpha=0.2f))
val ErrorRed = Color(0xFFFF4D4D) // Vivid error red

val GoldActive = Color(0xFF00E997) // No more classic gold, replaced with PineLauncher green!
val MutedSlate = Color(0x26FFFFFF) // Muted border alpha 15%

